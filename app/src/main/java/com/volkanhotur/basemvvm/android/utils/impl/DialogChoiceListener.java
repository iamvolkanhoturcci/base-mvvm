package com.volkanhotur.basemvvm.android.utils.impl;

import android.app.Dialog;

public interface DialogChoiceListener {
    void positiveClickListener(Dialog dialog);
    void negativeClickListener(Dialog dialog);
}
